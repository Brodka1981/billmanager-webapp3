import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.duckdns.gestionebollette',
  appName: 'gestionebollette',
  webDir: 'dist/bill-manager-frontend/browser'
};

export default config;
