import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gestionebollette.duckdns.org',
  appName: 'gestionebollette',
  webDir: 'dist/bill-manager-frontend/browser'
};

export default config;
